// The initial state of the App
const initialState = {
};

function buyReducer(state = initialState, action) {
  switch (action.type) {
    default:
      return state;
  }
}

export default buyReducer;
